import 'package:flutter/material.dart';

class Classes_practice extends StatelessWidget {
  const Classes_practice({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: TextField(),
    );
  }
}
